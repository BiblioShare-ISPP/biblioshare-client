language: node_js
node_js:
  - "10.16.3"
branches:
  only:
  - master
install:
- npm install
- npm install -g firebase-tools
script:
- npm run build
- echo "Deploying"
after-success:
- firebase deploy --token $FIREBASE_TOKEN